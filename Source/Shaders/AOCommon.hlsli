#ifndef WON_AO_COMMON
#define WON_AO_COMMON

static const float ao_radius = 0.7f; // view space radius(same as world space radius)
static const float ao_intensity = 1.0f;
static const float ao_bias = 0.01f;

#endif // WON_AO_COMMON
